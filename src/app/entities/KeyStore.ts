export class KeyStore {

    constructor(keystore, id, name) {
        this.keystore = keystore;
        this.id = id;
        this.name = name;
    }

    keystore: string;
    id: string;
    name: string;

}
